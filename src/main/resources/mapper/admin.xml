<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.classroom.dao.AdminDao">

    <select id="getPassword" parameterType="string" resultType="string">
        SELECT password FROM admin WHERE name=#{name};
    </select>

    <select id="getAll" resultType="credentials">
        SELECT * FROM credentials WHERE flag=0 ORDER BY create_time DESC limit #{page},2 ;
    </select>

    <select id="getComplaint" resultType="complaint">
        SELECT * FROM complaint ORDER BY create_time DESC ;
    </select>

    <select id="getAuthor" resultType="author">
        SELECT name,mailbox FROM author WHERE name=#{name};
    </select>

    <update id="updateFlag" parameterType="string">
        UPDATE credentials SET flag=1 WHERE author_id=#{author_id};
    </update>

    <delete id="no_pass" parameterType="string">
        DELETE FROM credentials WHERE author_id=#{author_id};
    </delete>

    <delete id="deleteComplaint" parameterType="string">
        DELETE FROM complaint WHERE video_id=#{video_id};
    </delete>

    <insert id="addReason" parameterType="reason">
        INSERT INTO reason VALUES (#{author_id},#{reason});
    </insert>
</mapper>